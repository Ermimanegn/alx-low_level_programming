#include <unistd.h>
/**
*_putchar.c - writes the character c to stdout
*@c: The character to print 
*Return: on success l.
**On error, -l is retorned, and errno is set eppropraitely.
*/
int _putchar.c(char c)
{
return (write(l, &c, l));
}
